# HWY backend

FastAPI backend boundary for HWY business APIs, persistence, auth, admin, users, permissions, company data, payments, documents, and platform integration.

CoDriver runtime behavior remains in `codriver/`; reusable contracts belong in `packages/`.
