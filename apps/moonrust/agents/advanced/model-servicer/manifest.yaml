agent_id: advanced.model-servicer
name: Model Servicer
category: advanced
capability_domain: serves and manages ML model inference
mission: I load, serve, and manage ML models for inference.
inputs:
- '{op: predict|explain|health|list, model: string, input: any, version?: string, explain?: bool, options?: {batch, timeout}}'
outputs:
- '{predictions: any, confidence?: float, explanation?: object, model: string, version: string, latency: int}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- config-loader for model configuration
- health-prober for model health
- metrics-collector for inference metrics
workers_needed:
- TODO
truth_rules:
- I load, serve, and manage ML models for inference. I support model versioning, A/B testing, GPU acceleration, batching, and model warm-up. I provide prediction explanations and confidence calibration
- 'out: {predictions: any, confidence?: float, explanation?: object, model: string, version: string, latency: int}'
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/advanced/model-servicer/ARCHITECTURE.md
