agent_id: utility.retry-queue
name: Retry Queue
category: utility
capability_domain: enqueue failed operations, schedule retries with backoff, and track retry attempts and outcomes
mission: I enqueue failed operations, schedule retries with backoff, and track retry attempts and outcomes.
inputs:
- '{op: enqueue|dequeue|retry|status, operation, max_retries?, backoff?} / out: {ok, queue_length, retry_count, next_retry_at}'
outputs:
- '{ok, queue_length, retry_count, next_retry_at}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- retry-agent, dead-letter-queue, scheduler
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/utility/retry-queue/ARCHITECTURE.md
