agent_id: network.packet-capturer
name: Packet Capturer
category: network
capability_domain: capture packets on interfaces, apply BPF filters, decode protocols, extract metadata, and save PCAP files
mission: I capture packets on interfaces, apply BPF filters, decode protocols, extract metadata, and save PCAP files.
inputs:
- '{op: capture|analyze|replay, interface?, filter?} / out: {packets, bytes, duration, protocols, file?}'
outputs:
- '{packets, bytes, duration, protocols, file?}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- network-manager, bandwidth-monitor, intrusion-detector
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/network/packet-capturer/ARCHITECTURE.md
