agent_id: devops.config-syncer
name: Config Syncer
category: devops
capability_domain: sync configs, secrets, and settings between environments
mission: I sync configs, secrets, and settings between environments.
inputs:
- '{op: sync|diff|promote|rollback, source, targets} / out: {ok, synced, conflicts, diffs}'
outputs:
- '{ok, synced, conflicts, diffs}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- config-map-manager, secret-syncer, drift-detector
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/devops/config-syncer/ARCHITECTURE.md
