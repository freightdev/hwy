agent_id: security.mfa-manager
name: Mfa Manager
category: security
capability_domain: enroll, verify, and manage MFA methods (TOTP, SMS, email, push, hardware keys, biometrics)
mission: I enroll, verify, and manage MFA methods (TOTP, SMS, email, push, hardware keys, biometrics).
inputs:
- '{op: enroll|verify|challenge|recovery|remove, user} / out: {ok, enrolled_methods, recovery_codes?}'
outputs:
- '{ok, enrolled_methods, recovery_codes?}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- otp-generator, identity-manager, notification-dispatcher
workers_needed:
- TODO
truth_rules:
- I enroll, verify, and manage MFA methods (TOTP, SMS, email, push, hardware keys, biometrics)
- 'in: {op: enroll|verify|challenge|recovery|remove, user} / out: {ok, enrolled_methods, recovery_codes?}'
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/security/mfa-manager/ARCHITECTURE.md
