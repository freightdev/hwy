agent_id: security.session-manager
name: Session Manager
category: security
capability_domain: create, validate, refresh, revoke sessions
mission: I create, validate, refresh, revoke sessions.
inputs:
- '{op: create|validate|refresh|revoke|list, session_id?} / out: {ok, session, active_sessions}'
outputs:
- '{ok, session, active_sessions}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- token-manager, cache-operator, audit-recorder
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/security/session-manager/ARCHITECTURE.md
