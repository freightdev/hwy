agent_id: data.data-profiler
name: Data Profiler
category: data
capability_domain: analyze datasets to compute statistics, detect data types, identify patterns, and profile distributions
mission: I analyze datasets to compute statistics, detect data types, identify patterns, and profile distributions.
inputs:
- '{data, profile_types?: stats|types|distributions|quality, max_samples?} / out: {columns: [{name, type, unique, nulls, min, max, mean, stddev}]}'
outputs:
- '{columns: [{name, type, unique, nulls, min, max, mean, stddev}]}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- data-validator, data-normalizer, data-quality-checker
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/data/data-profiler/ARCHITECTURE.md
