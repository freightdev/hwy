agent_id: system.package-manager
name: Package Manager
category: system
capability_domain: install, remove, update, and query packages
mission: I install, remove, update, and query packages.
inputs:
- '{op: install|remove|update|search|verify, packages} / out: {ok, installed, removed, total_size}'
outputs:
- '{ok, installed, removed, total_size}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- system-updater, dependency-updater, file-manager
workers_needed:
- TODO
truth_rules:
- 'in: {op: install|remove|update|search|verify, packages} / out: {ok, installed, removed, total_size}'
- 'backend: apt|yum|pacman|brew|pip|npm, default_repos, verify_gpg, no_recommends'
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/system/package-manager/ARCHITECTURE.md
