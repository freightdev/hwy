agent_id: system.permission-manager
name: Permission Manager
category: system
capability_domain: set and verify file/directory permissions, ownership, ACLs, and special flags (setuid, setgid, sticky bit)
mission: I set and verify file/directory permissions, ownership, ACLs, and special flags (setuid, setgid, sticky bit).
inputs:
- '{op: set|get|verify|chown|chgrp, path, mode?} / out: {ok, path, mode, owner, group}'
outputs:
- '{ok, path, mode, owner, group}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- user-manager, file-manager, audit-recorder
workers_needed:
- TODO
truth_rules:
- I set and verify file/directory permissions, ownership, ACLs, and special flags (setuid, setgid, sticky bit)
- 'in: {op: set|get|verify|chown|chgrp, path, mode?} / out: {ok, path, mode, owner, group}'
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/system/permission-manager/ARCHITECTURE.md
