agent_id: communication.grpc-client
name: Grpc Client
category: communication
capability_domain: send unary, server-streaming, client-streaming, and bidirectional-streaming gRPC calls
mission: I send unary, server-streaming, client-streaming, and bidirectional-streaming gRPC calls.
inputs:
- '{service, method, request, metadata?} / out: {response, trailing_metadata?, status}'
outputs:
- '{response, trailing_metadata?, status}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- rpc-caller, service-mesh, connection-pooler
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/communication/grpc-client/ARCHITECTURE.md
