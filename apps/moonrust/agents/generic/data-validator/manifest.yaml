agent_id: generic.data-validator
name: Data Validator
category: generic
capability_domain: validates data against schemas and rules
mission: I validate structured data against JSON Schema, YAML Schema, or custom rule sets.
inputs:
- '{data: any, schema: object|ref, format: string, coerce?: bool}'
outputs:
- '{valid: bool, errors?: [{path, message, code}], data?: any}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- file-manager for loading schema files
- config-loader for validation config
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/generic/data-validator/ARCHITECTURE.md
