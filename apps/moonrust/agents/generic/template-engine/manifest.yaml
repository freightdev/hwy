agent_id: generic.template-engine
name: Template Engine
category: generic
capability_domain: renders templates with provided data
mission: I take a template string or reference and a data context, then render the template by substituting variables, executing conditionals, and iterating collections.
inputs:
- '{template: string|ref, data: object, syntax: string, partials?: {name: string}}'
outputs:
- '{result: string, errors?: []}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- file-manager for loading template files
- config-loader for syntax config
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/generic/template-engine/ARCHITECTURE.md
