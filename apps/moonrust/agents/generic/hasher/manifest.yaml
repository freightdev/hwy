agent_id: generic.hasher
name: Hasher
category: generic
capability_domain: computes hashes and checksums
mission: I compute cryptographic hashes (SHA-256, SHA-512, BLAKE3, MD5), checksums (CRC32, Adler-32), and HMACs.
inputs:
- TODO
outputs:
- '{hash?: string, match?: bool, algorithm: string}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- file-manager for file hashing
- Cryptography libraries
workers_needed:
- TODO
truth_rules:
- I compute cryptographic hashes (SHA-256, SHA-512, BLAKE3, MD5), checksums (CRC32, Adler-32), and HMACs. I can hash data directly or verify files against expected hashes
- 'in (verify): {op: verify, data: string|bytes, hash: string, algorithm: string}'
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/generic/hasher/ARCHITECTURE.md
