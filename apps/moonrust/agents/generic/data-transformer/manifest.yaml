agent_id: generic.data-transformer
name: Data Transformer
category: generic
capability_domain: converts data between formats
mission: I transform data between JSON, YAML, TOML, CSV, XML, and other structured formats.
inputs:
- '{data: any, from_format: string, to_format: string, mapping?: object, filter?: string}'
outputs:
- '{data: any, warnings?: []}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- config-loader for format config
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/generic/data-transformer/ARCHITECTURE.md
