agent_id: specialized.secret-keeper
name: Secret Keeper
category: specialized
capability_domain: securely manages secrets and credentials
mission: I store, retrieve, rotate, and inject secrets (API keys, passwords, certificates, tokens).
inputs:
- '{op: store|retrieve|rotate|delete|list, key: string, value?: string|object, provider?: string}'
outputs:
- '{ok: bool, value?: string, keys?: [], rotated?: bool}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- cache-operator for secret caching
- audit-recorder for access logging
- encryptor for encryption operations
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/specialized/secret-keeper/ARCHITECTURE.md
