agent_id: specialized.timezone-converter
name: Timezone Converter
category: specialized
capability_domain: converts times between timezones
mission: I convert datetime values between timezones, detect timezone from location, handle DST transitions, and format times in various ISO and locale-specific formats.
inputs:
- '{time: string, from_tz?: string, to_tz: string, format?: string, locale?: string}'
outputs:
- '{input: {time, tz}, output: {time, tz, utc_offset, dst_active}, converted: string, formats: {iso, locale}}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- config-loader for timezone configuration
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/specialized/timezone-converter/ARCHITECTURE.md
