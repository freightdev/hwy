agent_id: specialized.api-gateway
name: Api Gateway
category: specialized
capability_domain: routes and proxies API requests
mission: I receive API requests, validate them, route to appropriate handlers, aggregate responses, handle authentication, enforce rate limits, and log all traffic.
inputs:
- '{method: string, path: string, headers: object, body: any, params: object, auth?: object}'
outputs:
- '{status: int, headers: object, body: any, elapsed: int}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- auth-manager for authentication
- rate-guard for rate limiting
- audit-recorder for request logging
- config-loader for route definitions
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/specialized/api-gateway/ARCHITECTURE.md
