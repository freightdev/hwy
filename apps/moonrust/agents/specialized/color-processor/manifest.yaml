agent_id: specialized.color-processor
name: Color Processor
category: specialized
capability_domain: manipulates and converts color values
mission: I convert between color spaces (RGB, HEX, HSL, HSV, CMYK, LAB), generate palettes, adjust brightness/saturation/contrast, and calculate color contrast ratios.
inputs:
- '{op: convert|palette|adjust|contrast, color: string, from_format?: string, to_format?: string, adjustments?: object}'
outputs:
- '{colors: {format: value}, palette?: [], contrast_ratio?: float, accessible?: bool}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- config-loader for color definitions
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/specialized/color-processor/ARCHITECTURE.md
