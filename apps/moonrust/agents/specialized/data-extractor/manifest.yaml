agent_id: specialized.data-extractor
name: Data Extractor
category: specialized
capability_domain: extracts structured data from unstructured text
mission: I use patterns, NLP, and LLM prompts to extract structured data (entities, relationships, dates, amounts, codes) from unstructured text.
inputs:
- '{text: string, schema: object, method?: regex|nlp|llm, hints?: object}'
outputs:
- '{data: object, confidence: float, fields: [{name, value, confidence, span?}], warnings?: []}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- llm-request for LLM-based extraction
- file-manager for loading pattern files
- data-validator for schema validation
workers_needed:
- TODO
truth_rules:
- 'out: {data: object, confidence: float, fields: [{name, value, confidence, span?}], warnings?: []}'
- 'confidence_threshold: minimum confidence score'
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/specialized/data-extractor/ARCHITECTURE.md
