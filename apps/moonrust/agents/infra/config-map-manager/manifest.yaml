agent_id: infra.config-map-manager
name: Config Map Manager
category: infra
capability_domain: create, update, and distribute configuration maps across environments
mission: I create, update, and distribute configuration maps across environments.
inputs:
- '{op: create|update|delete|diff|rollback, name, data} / out: {ok, config_ref, version, diff?}'
outputs:
- '{ok, config_ref, version, diff?}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- kubernetes-manager, schema-validator, config-syncer
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/infra/config-map-manager/ARCHITECTURE.md
