agent_id: infra.certificate-manager
name: Certificate Manager
category: infra
capability_domain: request, renew, deploy, and revoke SSL/TLS certificates
mission: I request, renew, deploy, and revoke SSL/TLS certificates.
inputs:
- '{op: request|renew|deploy|revoke|list, domain} / out: {ok, certificate: {domain, issuer, expiry, fingerprint}}'
outputs:
- '{ok, certificate: {domain, issuer, expiry, fingerprint}}'
allowed_callers:
- TODO
allowed_actors:
- TODO
required_permissions:
- TODO
risk_level: TODO
tools_needed:
- dns-manager, load-balancer, secret-keeper
workers_needed:
- TODO
truth_rules:
- On wake, I load my configuration, verify my dependencies are available, register my capabilities with the orchestrator, and await instructions
logbooks_written:
- TODO
reports_contributed_to:
- TODO
human_review_required_for:
- TODO
cannot_do:
- TODO
architecture_source: moonrust/agents/infra/certificate-manager/ARCHITECTURE.md
